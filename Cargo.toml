[package]
name = "clean_architecture_rs"
version = "0.1.0"
edition = "2018"

[dependencies]
actix-web = "3"

# grpc
tonic = "0.5"
prost = "0.8"
tokio = { version = "1.0", features = ["macros", "rt-multi-thread"] }

[build-dependencies]
tonic-build = "0.5"
